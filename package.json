{
  "name": "studio-meadow-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "npm run dev",
    "check": "npm --prefix apps/backend run check && npm --prefix apps/frontend run build"
  }
}
